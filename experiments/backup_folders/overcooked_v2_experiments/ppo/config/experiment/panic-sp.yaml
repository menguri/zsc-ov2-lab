# @package _global_
# Panic variant of rnn-sp: partner random action during specified window.
# Overrides model to RNN and enables panic with default window.

defaults:
  - override /model: rnn

panic:
  enabled: true
  start_step: 50    # Default activation step
  duration: 30      # Default duration (steps) after start
